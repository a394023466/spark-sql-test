package com.lyz.spark

object SparkParquet {
 /* def main(args: Array[String]): Unit = {


    val spark = SparkSession.builder().appName("DataInput").master("local[2]").getOrCreate()

    spark.sql("select * from parquet.`parquet文件路径`")
    spark.sql("select name,explode(num) from table")

  }*/
}
